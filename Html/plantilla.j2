<!DOCTYPE html>
<html>
<head>
    <title>CARTON DE BINGO</title>
    <meta charset="UTF-8">
</head>

<style>
body {
    margin: 0;
    padding: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    height: 100vh;
    background-color: black
}

.table {
    display: flex;
    flex-direction: column;
}

.fila {
    display: flex;
}

.fila > div {
    border: 1px solid #D1FEE8;
    width: 4em;
    height: 4em;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2em;
    color: #D1FEE8;
    text-align: center;
}

.celda {
    background: #1CC874;
    color: black;
}

.vacia {
    background: #8AE8BA;
}



</style>

<body>
    <div class="tabla">
        {% for fila in carton %}
        <div class="fila">
            {% for celda in fila %}
            {% if celda == 0 %}
            <div class="vacia">
            </div>
            {% else %}
            <div class="celda">
                {{ celda }}
            </div>
            {% endif %}
            {% endfor %}
        </div>
        {% endfor %}
    </div>
</body>
</html>